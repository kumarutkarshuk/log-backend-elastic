filebeat.inputs:
  - type: filestream
    paths:
      - /app/logs/*.log

output.elasticsearch:
  hosts: [ "${ELASTIC_HOST}" ]
  api_key: "${ELASTIC_API_KEY}"

setup.template.enabled: false
setup.ilm.enabled: false